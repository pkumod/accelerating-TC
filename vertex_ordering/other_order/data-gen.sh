#!/bin/bash
	echo "./reorder serializeData/kron-log16 -order bfsr"
	./reorder serializeData/kron-log16 -order bfsr
	echo "./reorder serializeData/cit-Patents -order bfsr"
	./reorder serializeData/cit-Patents -order bfsr
	echo "./reorder serializeData/com-lj -order bfsr"
	./reorder serializeData/com-lj -order bfsr
	echo "./reorder serializeData/email-enron -order bfsr"
	./reorder serializeData/email-enron -order bfsr
	echo "./reorder serializeData/email-euall -order bfsr"
	./reorder serializeData/email-euall -order bfsr
	echo "./reorder serializeData/gowalla -order bfsr"
	./reorder serializeData/gowalla -order bfsr
	echo "./reorder serializeData/kron-log21 -order bfsr"
	./reorder serializeData/kron-log21 -order bfsr
	echo "./reorder serializeData/road_central -order bfsr"
	./reorder serializeData/road_central -order bfsr
	echo "./reorder serializeData/soc-live -order bfsr"
	./reorder serializeData/soc-live -order bfsr
	echo "./reorder serializeData/wiki-topcats -order bfsr"
	./reorder serializeData/wiki-topcats-seri -order bfsr

	echo "./reorder serializeData/kron-log16 -order metis"
	./reorder serializeData/kron-log16 -order metis
	echo "./reorder serializeData/cit-Patents -order metis"
	./reorder serializeData/cit-Patents -order metis
	echo "./reorder serializeData/com-lj -order metis"
	./reorder serializeData/com-lj -order metis
	echo "./reorder serializeData/email-enron -order metis"
	./reorder serializeData/email-enron -order metis
	echo "./reorder serializeData/email-euall -order metis"
	./reorder serializeData/email-euall -order metis
	echo "./reorder serializeData/gowalla -order metis"
	./reorder serializeData/gowalla -order metis
	echo "./reorder serializeData/kron-log21 -order metis"
	./reorder serializeData/kron-log21 -order metis
	echo "./reorder serializeData/road_central -order metis"
	./reorder serializeData/road_central -order metis
	echo "./reorder serializeData/soc-live -order metis"
	./reorder serializeData/soc-live -order metis
	echo "./reorder serializeData/wiki-topcats -order metis"
	./reorder serializeData/wiki-topcats-seri -order metis

	echo "./reorder serializeData/kron-log16 -order slashburn"
	./reorder serializeData/kron-log16 -order slashburn
	echo "./reorder serializeData/cit-Patents -order slashburn"
	./reorder serializeData/cit-Patents -order slashburn
	echo "./reorder serializeData/com-lj -order slashburn"
	./reorder serializeData/com-lj -order slashburn
	echo "./reorder serializeData/email-enron -order slashburn"
	./reorder serializeData/email-enron -order slashburn
	echo "./reorder serializeData/email-euall -order slashburn"
	./reorder serializeData/email-euall -order slashburn
	echo "./reorder serializeData/gowalla -order slashburn"
	./reorder serializeData/gowalla -order slashburn
	echo "./reorder serializeData/kron-log21 -order slashburn"
	./reorder serializeData/kron-log21 -order slashburn
	echo "./reorder serializeData/road_central -order slashburn"
	./reorder serializeData/road_central -order slashburn
	echo "./reorder serializeData/soc-live -order slashburn"
	./reorder serializeData/soc-live -order slashburn
	echo "./reorder serializeData/wiki-topcats -order slashburn"
	./reorder serializeData/wiki-topcats-seri -order slashburn

